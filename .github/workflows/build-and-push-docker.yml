name: Build And Push Docker

on:
  workflow_call:
    inputs:
      repository-name:
        description: "Name of the DockerHub repository"
        required: true
        type: string
      image-tag:
        description: "Docker image tag"
        required: true
        type: string
      local-path:
        description: "Path to the directory containing Dockerfile"
        required: true
        type: string
    secrets:
      DOCKERHUB_USERNAME:
        required: true
      DOCKERHUB_TOKEN:
        required: true

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout PR source branch
        uses: actions/checkout@v4
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: miloss01
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Build and push Docker image
        run: |
          echo "docker build -t miloss01/${{ inputs.repository-name }}:${{ inputs.image-tag }} ${{ inputs.local-path }}"
          echo "docker push miloss01/${{ inputs.repository-name }}:${{ inputs.image-tag }}"
          docker build -t miloss01/${{ inputs.repository-name }}:${{ inputs.image-tag }} ${{ inputs.local-path }}
          docker push miloss01/${{ inputs.repository-name }}:${{ inputs.image-tag }}
